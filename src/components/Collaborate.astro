---
import GlassCard from "./GlassCard.astro";

const audiences = [
  {
    title: "Pharmaceutical companies",
    paragraphs: [
      "We produce high-grade outdoor cannabis flower under EU-GACP compliance, aimed at full EU-GMP compatibility through controlled post-harvest curing. Talk to us about supply or long-term sourcing agreements.",
    ],
  },
  {
    title: "Seed banks and breeders",
    paragraphs: [
      "We are building a genetic and chemotype library with full tracking of genotype, phenotype and environment. We welcome research collaborations, joint breeding programmes and field trials under formal agreement.",
    ],
  },
  {
    title: "Investors and partners",
    paragraphs: [
      "We are building Siren with people who see cannabis as a stable, medical-grade crop, and Portugal as the place to prove it. If that sounds like you, we would like to talk.",
    ],
  },
];
---

<div class='collab'>
  <div class='collab-header'>
    <h2>How we collaborate</h2>
    <p>
      Long-term relationships across the medical cannabis chain.
    </p>
  </div>

  <div id='collab-grid'>
    {
      audiences.map((audience) => (
        <GlassCard>
          <div class='collab-body'>
            <h3>{audience.title}</h3>
            {audience.paragraphs.map((paragraph) => (
              <p>{paragraph}</p>
            ))}
          </div>
        </GlassCard>
      ))
    }
  </div>
</div>

<style>
  /* a new topic begins here; major section break */
  .collab {
    padding: clamp(96px, 10vw, 168px) 0 0;
  }
  .collab-header {
    max-width: 52em;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 14px;
  }
  .collab-header h2 {
    margin: 0;
    font-size: clamp(36px, 3.2vw, 52px);
    line-height: 1.12;
    color: var(--color-saffron-mango);
  }
  .collab-header p {
    margin: clamp(6px, 1vw, 14px) 0 0;
    max-width: 36em;
    font-size: clamp(17px, 1.45vw, 22px);
    line-height: 1.5;
    color: var(--color-white);
    text-wrap: pretty;
  }
  #collab-grid {
    margin-top: clamp(36px, 4vw, 60px);
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: clamp(20px, 2.2vw, 32px);
    align-items: stretch;
  }
  @media (max-width: 1100px) {
    #collab-grid {
      grid-template-columns: 1fr;
    }
  }
  .collab-body {
    display: flex;
    flex-direction: column;
    gap: 18px;
  }
  .collab-body h3 {
    margin: 0;
    font-size: clamp(28px, 2.4vw, 36px);
    line-height: 1.25;
    color: var(--color-saffron-mango);
  }
  .collab-body p {
    margin: 0;
    font-size: clamp(16px, 1.5vw, 20px);
    line-height: 1.4;
    color: var(--color-white);
  }
</style>
